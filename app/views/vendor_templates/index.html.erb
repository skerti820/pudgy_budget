<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All vendors
    </h1>

    <a href="/vendors/new" class="btn btn-block btn-outline-secondary">
      Add a new vendor
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "vendor_templates/vendors_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Name
        </th>

        <th>
          Deleted
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @vendors.each do |vendor| %>
      <tr>
        <!-- Display vendor.id start -->
        <td>
          <%= vendor.id %>
        </td>
        <!-- Display vendor.id end -->

        <!-- Display vendor.name start -->
        <td>
          <%= vendor.name %>
        </td>
        <!-- Display vendor.name end -->

        <!-- Display vendor.deleted start -->
        <td>
          <%= vendor.deleted %>
        </td>
        <!-- Display vendor.deleted end -->

        <!-- Display vendor.created_at start -->
        <td>
          <%= time_ago_in_words(vendor.created_at) %> ago
        </td>
        <!-- Display vendor.created_at end -->

        <!-- Display vendor.updated_at start -->
        <td>
          <%= time_ago_in_words(vendor.updated_at) %> ago
        </td>
        <!-- Display vendor.updated_at end -->

        <td>
          <a href="/vendors/<%= vendor.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @vendors, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
